## v0.7.9（2026-09-08）

> `0.7.9` 是基于上游 `v0.7.8` 的下游版本，并非上游发布版本；本次 LazyCat 包修订号为 `0.7.9-5`。

### 新增
- 增加 LazyCat 同源持久化服务，兼容前端完整快照同步，并保留实验性的 CAS、版本化元数据和范围读取接口。
- 增加跨进程文件锁、原子写入、同源校验、会话 Cookie 与 CSRF 防护测试。

### 变更
- 将源码整理为标准根目录布局，移除旧版嵌套源码与历史部署手册。
- 将公开默认 API 地址恢复为 `https://api.openai.com/v1`，默认关闭且不锁定同源代理。
- 将服务端脱敏恢复副本放入私有 `PERSISTENCE_DIR/backups`，不再预置个人文档路径。
- 将公开主页更新为下游仓库，并仅保留测试型 CI，不包含自动部署工作流。

### 修复
- 修复前端快照同步端点与持久化运行时不一致导致的 `404` 请求失败。
- 修复并发服务进程可能覆盖快照更新的问题。
